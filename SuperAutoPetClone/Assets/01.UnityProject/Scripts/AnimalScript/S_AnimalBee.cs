using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class S_AnimalBee : S_Animal
{

    // Start is called before the first frame update
    void Start()
    {
        HP = 1;
        Damage = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
