using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class S_AnimalAnt : S_Animal
{
    // Start is called before the first frame update
    void Start()
    {
        HP = 1;
        Damage = 2;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //! 스킬 사용
    void ActiveSkill()
    {

    }

    //! 데미지를 받았을 경우
    
}
